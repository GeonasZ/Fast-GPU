# GPU 云厂商选型需求与淘汰记录

> 状态：长期维护的选型基线与禁止重复推荐清单  
> 最后更新：2026-07-18  
> 原始任务：[WhaleTech Take Home Exam（AI infra）](https://whaletech.notion.site/Take-Home-Exam-AI-infra-3365f589aa8080cb848aecf37d03790f)

## 1. 文档目的

本文档用于后续 GPU 云厂商调研、接入和复审。任何新厂商必须先通过硬性门槛，再进入 API 实测和小额付费验证。已淘汰厂商不得再次推荐，除非有可验证的产品或支付机制变化，并按本文的解禁流程重新评审。

## 2. 项目目标与范围

建设公司内部 GPU 调度平台，整体体验对标 Vast.ai 的“资源展示 → 选择配置 → 一键开机 → 实例可用性与监控”，但不绑定 Vast.ai。平台至少同时接入 3 家供应商，向用户提供统一的资源发现、报价、创建、生命周期、监控和报告界面。Slurm 调度属于可选项。

原任务预计 5 天内完成或尽可能取得进展；演示时重视实际成果、功能完成度、遇到的问题及解决过程。初始实测预算为 100 元人民币，必要时可申请增加。

## 3. 一票否决项（Hard Gates）

候选厂商必须同时满足以下条件；任一项不满足即淘汰，不进入价格排序。

### 3.1 中国大陆可完成付款

- 必须至少支持一种实际可用的中国大陆付款路径：支付宝、微信支付、中国大陆银行卡、人民币余额充值或境内对公汇款。
- 仅支持 Stripe、境外信用卡、PayPal、加密货币或境外电汇，不合格。
- 不能只依据帮助文档；必须以人民币小额充值或真实付款页面完成一次验证。
- 必须记录最低充值额、退款规则、发票能力、余额有效期和欠费/自动停机规则。

### 3.2 必须真实支持 CUDA 13

- 必须能启动 Ubuntu 24.04，并运行 CUDA 13 用户态和兼容驱动。
- 必须能直接运行 NVIDIA NGC `nvcr.io/nvidia/pytorch:26.03-py3`，或允许制作等价的私有镜像/启动模板。
- “宿主机驱动较新”“控制台可选择 CUDA”或“理论上向后兼容”不能单独作为通过依据。
- 若厂商只提供固定容器/镜像且没有 CUDA 13，直接淘汰；不能依靠启动后覆盖系统 CUDA 来掩盖宿主驱动不兼容。

CUDA 13 实机验收命令至少包括：

```bash
nvidia-smi
nvcc --version
python - <<'PY'
import torch
print(torch.__version__)
print(torch.version.cuda)
print(torch.cuda.is_available())
print(torch.cuda.get_device_name(0))
PY
```

同时必须在目标 NGC 容器内完成一次 CUDA kernel/PyTorch GPU 运算。验收记录须保留实例型号、区域、镜像摘要、驱动版本、CUDA 版本和命令输出。

### 3.3 有可自动化的实例 API

- 必须有受支持的 API，用于资源/规格发现、创建、查询、启动、停止或释放实例。
- API 必须返回稳定实例 ID、生命周期状态和连接信息。
- 创建 API 必须支持启动脚本、cloud-init、容器命令或等价自动装机入口。
- 仅有网页控制台、依赖人工客服开机或禁止自动化，不合格。
- 必须确认 API 限流、鉴权方式、幂等/重试行为及错误结构。

### 3.4 GPU 型号与供应商数量

- 最终接入至少 3 家通过验收的供应商。
- 资源池需要覆盖 A100、H 系列卡和任务原文所写的“6090”。
- “6090”在采购前必须由需求方确认准确 SKU；若市场尚无该正式型号，不得擅自用 5090、4090 或其他型号冒充，只能标记为“待确认/暂不可满足”。
- H 卡至少记录具体型号（如 H100/H200）及显存、GPU 数量、互联形态，不能只显示“H 卡”。

### 3.5 预算可验证

- 应能在约 100 元人民币初始预算内完成至少一次创建、环境验收、网络测试、性能测试和释放流程。
- 厂商若有高额最低充值、最低租期、审批费或不可退余额，导致无法在预算内实测，则当前阶段淘汰或标记为“需预算豁免”。

## 4. 实例与平台功能要求

### 4.1 统一资源市场

- 聚合至少 3 家供应商，展示厂商、区域、GPU 型号/数量、显存、CPU、内存、磁盘、库存和可靠性信息。
- 报价必须携带原始币种与计费周期，不得把 USD 和 CNY 当作同一单位比较。
- 如果做统一人民币排序，必须展示汇率来源、抓取时间、原价和换算价；无汇率时按原币种分组。
- 实时价、缓存价、参考价和“创建时确认价”必须明确区分。
- 一键创建前显示最终配置、原币种价格、磁盘/公网 IP/流量等附加费用和停止后的计费规则。

### 4.2 生命周期与可用性

- 支持创建、查询、启动、停止和释放；若厂商不支持真正停止，应明确展示其实际语义。
- 必须区分“关机但继续收 GPU 费”“休眠仅收存储费”“释放后数据销毁”。
- 创建流程需要可重试，并能明确报告库存不足、配额不足、支付余额不足、镜像不兼容等错误。
- 实例可用后应自动注册 Agent 或安全连接地址，不能依赖人工复制粘贴才能完成监控。

### 4.3 开机即用环境

- Ubuntu 24.04。
- CUDA 13。
- NVIDIA NGC PyTorch `26.03-py3`。
- 最新稳定版 Codex CLI 和 Claude Code CLI。
- 自动安装或提供性能测试所需工具，包括 `nvbandwidth`、`fio` 和网络测速工具。

### 4.4 S3-compatible 数据流程

- 支持 S3-compatible 存储配置。
- 示例数据为 FineWeb-Edu `CC-MAIN-2013-20`，约 33 GB。
- 必须完成“离线入桶 → 实例在线加载 → 落到实例本地高速盘”的完整流程。
- 同步需要校验、断点/重试能力和可观察进度；凭据应限制 bucket/prefix、权限和有效期。
- 数据最终必须位于本地高速盘，不得把网络挂载伪装成本地导入完成。

### 4.5 性能报告（网页可展示、可导出）

每次报告必须记录实例 ID、厂商、区域、GPU/驱动/CUDA、测试时间和工具版本，并至少包含：

- 每张 GPU 的显存带宽。
- 每张 GPU 的 NVLink 带宽；无 NVLink 时明确写“不支持/不存在”，不得记为 0 伪装成实测。
- PCIe lanes 数量。
- 每张 GPU 的 PCIe 带宽。
- 互联网下载和上传速度。
- 本地磁盘带宽。

报告至少支持 JSON 导出；若提供 CSV/PDF 属于加分项。失败项目必须保留错误原因和原始输出摘要。

### 4.6 外网直连测试

不得通过镜像站或代理替代目标站点，必须分别测试：

- Hugging Face
- Cloudflare
- AWS
- OpenAI
- Google

记录 DNS、TCP/TLS 是否成功、延迟、HTTP 状态及失败原因。厂商或中国大陆网络政策导致不可达时必须如实显示，不得改测镜像站后判定为通过。

### 4.7 Telemetry UI

- 网页实时展示实例生命周期状态。
- 展示 GPU 利用率、显存使用、温度、功耗，以及主机 CPU、内存等关键资源。
- 断线、Agent 未注册和权限失败必须有不同状态，不能统一显示为 0。
- 建议记录最近更新时间并检测过期数据。

### 4.8 网络与安全（加分项，但生产接入前必须评审）

- 不直接暴露公网 22 端口；优先使用厂商代理、安全隧道、VPN 或限制源地址的安全组。
- Agent 必须鉴权，控制端与实例间使用 TLS 或可信隧道。
- S3 管理员密钥不得下发；使用最小权限、短期凭据。
- 输出网络和数据安全测试结论，包括开放端口、入站来源限制、凭据暴露面和实例释放后的数据处理。

## 5. 厂商实测证据清单

每个候选厂商建立独立记录，至少附上：

1. 官方产品、API、计费、付款与镜像文档链接及访问日期。
2. 中国大陆付款成功证据和实际支付币种（敏感信息需脱敏）。
3. API Token 创建方式及最小权限范围。
4. 可用 GPU SKU、区域、库存和含税/附加费用报价。
5. 实际创建与释放请求/响应（密钥脱敏）。
6. CUDA 13 与 NGC 26.03 实机验收输出。
7. 性能、网络、S3 和 Telemetry 报告。
8. 停止、休眠、释放、存储和公网 IP 的计费实测。
9. 已知限制、失败记录、客服确认及复审日期。

只做网页调研但未付费、未开机的厂商最多标记为“待实测”，不能标记为“通过”。

## 6. 厂商状态与禁止重复推荐清单

| 厂商 | 状态 | 淘汰/风险原因 | 禁止重复推荐条件 | 可解禁证据 |
|---|---|---|---|---|
| Vast.ai | **BANNED** | 付款机制不符合当前使用需求；用户已明确否决 | 后续搜索和推荐必须排除 | 中国大陆可实际完成的付款流程发生变化，并完成小额支付实测；仍需重新验证 CUDA 13、API 和完整任务 |
| Hyperstack | **REOPENED / 自动验收中** | 2026-07-18 用户确认控制台存在可用充值入口；平台已接入 flavor/pricebook、一键 VM、cloud-init、CUDA 自动判型、显式 12.8 降级和失败自动删除 | 实机验收完成前不得计入 3 家合格供应商；CUDA 12.8 降级只能标记为不满足原题 | 完成真实充值并由平台提交 CUDA 13.2、目标 NGC、生命周期、性能、网络和 S3 全套实测 |
| AutoDL | **BANNED** | 当前不提供满足要求的 CUDA 13 容器/镜像路径，无法达到开机即用的 CUDA 13 + NGC 26.03 要求 | 即使其支持微信/支付宝，也不得再次推荐为本项目合格供应商 | 官方提供 CUDA 13 容器，或实测证明其宿主驱动与自定义/私有镜像可以稳定运行目标 NGC 26.03；必须附完整命令输出 |
| PPIO 派欧云 | **CANDIDATE / 待完整实测** | 支持国内付款和 GPU API；CUDA 13、目标 NGC 镜像、完整性能/外网/S3 流程仍需按本文留证 | 不得直接宣称“已通过” | 完成付款、开机、CUDA 13、NGC、生命周期、报告、外网和 S3 全套实测 |
| RunPod | **PAYMENT BLOCKED / 待复核** | 已有 API 适配，但必须确认中国大陆实际付款路径；若只能使用当前不可用的境外卡/Stripe，则淘汰 | 未完成国内付款实测前不能计入 3 家合格供应商 | 完成中国大陆付款和全部技术验收；否则转为 BANNED |
| 潞晨云 | **RESEARCH QUEUE** | 原任务点名候选，尚未完成当前产品、API、付款和 CUDA 13 实测 | 不得凭名称直接计入供应商数量 | 官方资料 + 真实付款 + 实例 API + CUDA 13/NGC 26.03 全套实测 |
| 阿里云 GPU ECS | **RESEARCH QUEUE** | 国内付款/API 可行，但规格、镜像、磁盘、网络和价格组合复杂，100 元预算可行性未知 | 不得只因“大厂”直接判定通过 | 筛出预算内 SKU，并完成节省停机、CUDA 13、NGC、网络和自动化实测 |
| 腾讯云 GPU CVM | **RESEARCH QUEUE** | 国内付款/API 可行，但 GPU 库存、配额、镜像与预算可行性未知 | 不得只因支持微信付款直接判定通过 | 筛出预算内 SKU，并完成关机不收费策略、CUDA 13、NGC 和完整自动化实测 |

## 7. 推荐评分（仅对通过所有 Hard Gates 的厂商使用）

硬门槛未通过时不得用高分抵消。通过后按以下权重比较：

| 维度 | 权重 | 评价重点 |
|---|---:|---|
| CUDA 13、镜像与驱动稳定性 | 25% | NGC 26.03 成功率、镜像可维护性、驱动透明度 |
| API 和自动化能力 | 20% | 发现/创建/生命周期、幂等、错误、限流、启动脚本 |
| GPU 库存与型号覆盖 | 15% | A100、明确的 H 系列、“6090”需求澄清后的目标 SKU |
| 成本和计费透明度 | 15% | 原币种报价、附加费用、停止计费、最低充值、预算适配 |
| 网络与数据能力 | 10% | 五站直连、带宽、S3、本地高速盘 |
| 可观测性与性能证据 | 10% | Telemetry、性能指标、报告导出、原始证据 |
| 安全与支持 | 5% | 无公网 22、最小权限、审计、SLA/客服响应 |

## 8. 调研执行顺序

1. 先查本文件的 BANNED 表，排除已淘汰厂商。
2. 验证中国大陆付款路径；不满足立即停止调研。
3. 核验 CUDA 13、Ubuntu 24.04 和目标 NGC 镜像能力；不满足立即停止。
4. 核验实例 API 和自动装机入口。
5. 核验 GPU SKU、库存、预算和停止计费语义。
6. 用最小成本创建真实实例，执行完整验收。
7. 保存脱敏证据，更新厂商状态与复审日期。
8. 只有通过全部硬门槛的厂商才能进入评分和最终的至少 3 家接入名单。

## 9. 变更与解禁规则

- 新增厂商时必须更新第 6 节，不能只写在聊天记录或临时笔记中。
- BANNED 厂商默认永久排除；“听说已经支持”不构成解禁。
- 解禁必须附官方变更链接、访问日期和真实付费/开机证据，并重新执行所有 Hard Gates。
- 任何币种、CUDA、镜像、API 或计费规则变化，都要更新本文的日期和对应厂商状态。
- 若需求方确认“6090”为笔误，应在本文留下确认记录并替换为准确型号，不能静默修改。
