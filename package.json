{
  "name": "gpu-fleet-console",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "start:web": "node server.js",
    "start:local": "node client.js",
    "start:all": "node local-launcher.js",
    "stop:all": "node local-launcher.js --stop",
    "test": "node test/all.test.js",
    "check": "node --check server.js && node --check lib/auth-store.js && node --check agent/agent.js && node --check public/app.js && node --check public/reachability.js && node --check public/auth.js"
  },
  "devDependencies": {
    "electron": "^43.2.0"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1095.0",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "node-pty": "^1.1.0"
  }
}
